# Teacher Tools - 项目架构文档

## 📋 项目概述

**项目名称**: teacher_tools
**项目描述**: 教师工具应用 - 学生信息管理系统
**开发平台**: Android
**开发框架**: Flutter 3.35.5
**Dart SDK**: ^3.9.2
**组织标识**: com.teacher

## 🏗️ 技术栈

### 核心框架
- **Flutter**: 3.35.5 - 跨平台UI框架
- **Dart**: 3.9.2 - 编程语言

### 状态管理
- **provider**: ^6.1.2 - 轻量级状态管理方案
  - 简单易学
  - 官方推荐
  - 适合中小型应用

### 数据存储
- **sqflite**: ^2.3.0 - SQLite 数据库
  - 本地持久化存储
  - 成熟稳定的解决方案
  - 支持完整的关系型数据库操作
- **path_provider**: ^2.1.0 - 获取系统路径
- **path**: ^1.8.3 - 路径操作

### 路由导航
- **go_router**: ^14.6.2 - 声明式路由
  - 类型安全的路由
  - 深度链接支持
  - 简化的导航逻辑

### UI组件
- **Flutter Material Design**: 官方Material组件库
- **intl**: ^0.19.0 - 国际化和日期格式化

### 开发工具
- **flutter_lints**: ^5.0.0 - 代码规范检查

## 📂 项目结构

```
lib/
├── main.dart                      # 应用入口
│
├── models/                        # 数据模型层
│   ├── student.dart               # 学生数据模型
│   └── base_model.dart            # 基础模型类（可选）
│
├── database/                      # 数据库层
│   ├── database_helper.dart       # 数据库帮助类
│   ├── schemas.dart               # 数据库表结构定义
│   └── migrations.dart            # 数据库迁移（可选）
│
├── providers/                     # 状态管理层
│   ├── student_provider.dart      # 学生状态管理
│   └── app_provider.dart          # 全局状态（可选）
│
├── screens/                       # 页面/屏幕层
│   ├── home_screen.dart           # 主页/学生列表
│   ├── student_form_screen.dart   # 添加/编辑学生
│   ├── student_detail_screen.dart # 学生详情
│   └── settings_screen.dart       # 设置页面（可选）
│
├── widgets/                       # 自定义组件层
│   ├── student_card.dart          # 学生卡片组件
│   ├── search_bar.dart            # 搜索栏组件
│   └── empty_state.dart           # 空状态组件
│
├── services/                      # 服务层
│   ├── navigation_service.dart    # 导航服务（可选）
│   └── storage_service.dart       # 存储服务（可选）
│
├── utils/                         # 工具类层
│   ├── constants.dart             # 常量定义
│   ├── date_formatter.dart        # 日期格式化工具
│   └── validators.dart            # 验证工具
│
└── routes/                        # 路由配置
    └── app_router.dart            # 路由配置
```

## 🎯 架构模式

### 分层架构
```
┌─────────────────────────────────┐
│   Presentation Layer (UI)       │  ← screens/, widgets/
│   - 页面和组件                   │
├─────────────────────────────────┤
│   Business Logic Layer          │  ← providers/
│   - 状态管理和业务逻辑            │
├─────────────────────────────────┤
│   Data Layer                    │  ← models/, database/
│   - 数据模型和持久化              │
└─────────────────────────────────┘
```

### 数据流向
```
User Action → Widget → Provider → Database → Provider → Widget Update
```

## 💾 数据库设计原则

### 表设计规范
- 所有表都有主键 `id` (INTEGER, AUTO_INCREMENT)
- 时间戳字段：`created_at`, `updated_at` (TEXT, ISO8601格式)
- 软删除：`is_deleted` (BOOLEAN, 可选)
- 索引：为常用查询字段添加索引

### 数据类型映射
| Dart Type | SQLite Type |
|-----------|-------------|
| int       | INTEGER     |
| double    | REAL        |
| String    | TEXT        |
| bool      | INTEGER (0/1) |
| DateTime  | TEXT (ISO8601) |

## 🔧 代码规范

### 命名约定
- **文件名**: 小写加下划线 `student_form.dart`
- **类名**: 大驼峰 `StudentFormScreen`
- **变量名**: 小驼峰 `studentName`
- **常量**: 小驼峰前缀 `kMaxStudents`
- **私有成员**: 下划线前缀 `_privateMethod`

### 目录组织原则
- 按功能模块分层（models, providers, screens等）
- 每个文件只包含一个主要类
- 相关的辅助类放在同一文件
- 组件按复用性分类（screens vs widgets）

### 依赖注入
- 使用 `Provider` 进行依赖注入
- 在 `main.dart` 中注册所有 providers
- 使用 `ChangeNotifier` 或 `FutureProvider` 管理状态

## 🎨 UI/UX 原则

### Material Design 3
- 使用 Material 3 组件
- 遵循 Android 设计规范
- 支持深色模式（可选）

### 响应式设计
- 使用 `MediaQuery` 获取屏幕尺寸
- 使用 `LayoutBuilder` 构建自适应布局
- 考虑横竖屏切换

### 主题配置
- 主色调：待定义
- 字体：Material 默认字体
- 圆角、阴影遵循 Material 规范

## 📱 平台特定配置

### Android
- **最小 SDK**: 21 (Android 5.0)
- **目标 SDK**: 34 (Android 14)
- **权限配置**:
  - 存储权限（如需要导出数据）
- **应用图标**: 待设计
- **应用名称**: 教师工具

### 未来扩展（预留）
- iOS: `ios/` 目录
- Web: `web/` 目录
- Windows: `windows/` 目录

## 🚀 构建和部署

### 开发命令
```bash
flutter run                    # 运行调试版本
flutter run --release         # 运行发布版本
flutter run -d <device_id>    # 指定设备运行
```

### 构建命令
```bash
flutter build apk --release   # 构建 APK
flutter build appbundle       # 构建 Android App Bundle
flutter build apk --split-per-abi  # 按架构拆分APK
```

### 代码分析
```bash
flutter analyze               # 静态代码分析
flutter test                  # 运行测试
flutter doctor                # 检查开发环境
```

## 📚 资源管理

### 图片资源
- 位置：`assets/images/`
- 格式：PNG, JPG
- 命名：小写加下划线
- 在 `pubspec.yaml` 中声明

### 字体资源（可选）
- 位置：`assets/fonts/`
- 在 `pubspec.yaml` 中配置

### 多语言支持（可选）
- 位置：`lib/l10n/`
- 使用 `flutter_localizations`
- 配置 `intl` 包

## 🧪 测试策略

### 单元测试
- 位置：`test/`
- 测试业务逻辑和工具类
- 目标覆盖率：>80%

### Widget测试
- 测试UI组件行为
- 使用 `testWidgets`

### 集成测试
- 端到端测试
- 位置：`integration_test/`

## 🔐 安全考虑

### 数据安全
- 数据库存储在应用私有目录
- 不存储敏感信息（密码等）
- 考虑数据加密（如需要）

### 输入验证
- 所有用户输入必须验证
- 防止SQL注入（使用参数化查询）
- 验证数据格式（手机号、邮箱等）

## 📊 性能优化

### 最佳实践
- 使用 `const` 构造函数
- 避免不必要的重建
- 使用 `ListView.builder` 而非 `ListView`
- 图片压缩和缓存

### 内存管理
- 及时释放资源
- 避免内存泄漏
- 使用 `devtools` 分析性能

## 🔄 版本控制

### Git工作流
- 主分支：`main` / `master`
- 功能分支：`feature/功能名`
- 修复分支：`fix/问题名`

### 提交规范
- feat: 新功能
- fix: 修复bug
- refactor: 重构
- style: 样式修改
- docs: 文档修改
- test: 测试相关
- chore: 构建/工具相关

## 📝 开发工作流

1. **需求分析** → 明确功能需求
2. **架构设计** → 设计数据模型和界面
3. **编码实现** → 按分层架构开发
4. **测试验证** → 单元测试和集成测试
5. **代码审查** → 检查代码质量
6. **部署发布** → 构建和发布

## 🎯 未来扩展方向

### 潜在功能模块
- 成绩管理
- 考勤记录
- 作业管理
- 课程表
- 数据统计和报表
- 云端同步
- 家校沟通

### 技术升级
- 考虑迁移到空安全（如果尚未）
- 引入更多自动化测试
- 集成CI/CD
- 性能监控和错误追踪

---

**文档版本**: 1.0.0
**最后更新**: 2025-12-24
**维护者**: Teacher Tools 开发团队
